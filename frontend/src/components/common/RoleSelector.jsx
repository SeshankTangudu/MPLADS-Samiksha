import React from 'react';

/**
 * RoleSelector is deprecated.
 * Authentication and role access are now governed by the genuine
 * Role-Based Authentication Gateway (/login?role=...).
 */
export const RoleSelector = () => {
  return null;
};

export default RoleSelector;
